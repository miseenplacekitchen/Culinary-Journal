# Scratch (local only)

Temporary files for Cursor/agent work — export audits, one-off scripts, unzipped `.xlsx` inspection, etc.

**Not part of the app.** Everything here is ignored by git except this README.

Put downloads and analysis artifacts here instead of `Downloads/` when reviewing exports or debugging.

Safe to delete anything in this folder at any time.
